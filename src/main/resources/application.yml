spring:
  h2:
    console:
      enabled: true
  devtools:
    livereload:
      enabled: true

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL57Dialect

  datasource:
    url : jdbc:h2:mem:testdb;MODE=MySQL